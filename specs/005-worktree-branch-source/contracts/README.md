# Contracts Directory

**功能说明**: 本功能是 CLI 工具的增强，不涉及外部 API 契约或 Web 服务接口。

本目录为空，因为：
- work CLI 工具是命令行界面，不是 Web 服务
- 本功能仅增强现有的 `create` 命令，不涉及 API 设计
- 所有交互通过命令行参数和终端输入/输出完成
- 无需 OpenAPI/GraphQL schema 或其他 API 契约文档

如有未来扩展（如创建 TUI 或 Web UI），可在此目录添加相关接口契约。
